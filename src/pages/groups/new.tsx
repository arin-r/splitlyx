import { FC } from "react";
import NewGroupForm from "~/components/NewGroupForm";

interface newProps {}

const page: FC<newProps> = ({}) => {
  return (
    <>
      <NewGroupForm />
    </>
  );
};

export default page;
